<h1> Settings</h1>
<%= link_to "Edit User", edit_user_path(current_user)%>
<% #TO DO: needs way to enter password when delting account%>
<%= link_to "Delete", user_path(current_user), method: 'delete', { data: confirm: "#{form_for @user do |f| f.text_field :password_confirmation}Are you sure?" } %>

